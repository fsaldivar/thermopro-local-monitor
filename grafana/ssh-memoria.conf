# Instalar en /etc/systemd/system/ssh.service.d/memoria.conf
#
# Sin acceso remoto no se puede diagnosticar nada. Que el asesino por falta
# de memoria vaya a por cualquier otro antes que a por sshd.
[Service]
OOMScoreAdjust=-900
